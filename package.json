{
    "name": "messenger",
    "private": true,
    "version": "0.0.0",
    "type": "commonjs",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "start": "tsc && vite build && node server.js  "
    },
    "devDependencies": {
        "express": "^4.18.2",
        "typescript": "^5.2.2",
        "vite": "^5.0.8"
    },
    "dependencies": {
        "handlebars": "^4.7.8"
    },
    "engines": {
        "node": ">=12.1.0"
    }
}
